import React from 'react';
import HoverModal from '../HoverModal';
import { AnimeDataModel } from '../../interface/AnimeDataModel';

const AnimeCards = () => {
   return <div>AnimeCards</div>;
};

export default AnimeCards;
